# 작업 로그 (2026-02-26)

## #2 JSON Import/Export 구현
- 상태: DONE
- 변경 파일: `TestData/SkillEditorWindow.cs`, `jules-queue.md`
- 작업 내용:
  - 스킬/시퀀스/에펙/컨디션 탭에 `JSON 가져오기`, `JSON 내보내기` 버튼 추가
  - 탭별 파일 매핑 구현 (`skill.json`, `sequence.json`, `affect.json`, `condition.json`)
  - 주석(`//`) 및 trailing comma를 허용하는 Relaxed JSON 파서 추가
  - 가져오기 시 ID/필드 로딩, `DESC` 기반 노드명 반영
  - 내보내기 시 현재 편집 상태를 JSON으로 저장 (라운드트립 가능)
- 검증(가능 범위):
  - 정적 점검: 파일 경로/파서/버튼 연결 코드 확인
  - Unity Editor 런타임 검증은 현재 세션에서 미실행

## #3 타입별 키 자동 폼 고도화
- 상태: DONE
- 변경 파일: `TestData/SkillEditorWindow.cs`, `jules-queue.md`
- 작업 내용:
  - `SimulatorTableItemSkill*.txt`에서 `eDataInt/eDataFloat/eDataString` enum 키 자동 추출
  - 스키마 수동 키 + enum 키를 합쳐 타입별 키 목록 자동 구성
  - 키 선택 시 대응 타입(`Int/Float/String`) 자동 보정
  - 값 형식 검증(INT/FLOAT) 경고 표시 추가
- 검증(가능 범위):
  - 정적 점검: enum 파싱/폼 바인딩/타입 보정 로직 확인
  - Unity Editor 런타임 검증은 현재 세션에서 미실행

## #4 인덱스 할당 규칙 엔진
- 상태: DONE
- 변경 파일: `TestData/SkillEditorWindow.cs`, `jules-queue.md`
- 작업 내용:
  - `IdRuleEngine` 추가: 규칙 인덱스(100/200/300/400/500~520/900~940) 기반 자동 할당
  - 탭 상단에 `캐릭터번호`, `수동 인덱스` 입력 UI 추가
  - `규칙ID 추가`, `수동ID 추가` 버튼 추가
  - 수동 인덱스는 규칙 외 값도 허용 (요구사항 반영)
- 검증(가능 범위):
  - 정적 점검: ID 계산/중복 감지/노드 생성 로직 확인
  - Unity Editor 런타임 검증은 현재 세션에서 미실행

